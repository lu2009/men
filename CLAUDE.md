# 项目约定

> 这是「开门红 / smartdoor」旧系统重建项目。目标：把混淆过的旧前端 bundle 逐条逆向，
> 在新栈（Rust axum + PostgreSQL + Vue3/TS/Naive UI）上复刻到 **≥95% 的 UI 与逻辑保真度**。
>
> 工作原则（用户定的）：**做减法** · **不要靠猜** · **全面完整** ·
> 先把逻辑/格式/语义**全部分析出来写进文档**，文档确认无误再动手。

---

## ⚠️ 硬规矩：改了行为，同一笔提交里必须改文档

**任何一笔改动，凡是让某个文档描述过的行为不再成立，必须在同一笔提交里改掉那个文档。**
文档里写的和代码不一样，就是 bug —— 与代码 bug **同级**，不是「回头再说」。

### 为什么有这条

这个项目已经因为「改完没回头改文档」栽过三次：

| # | 事故 | 代价 |
|---|---|---|
| 1 | 审计 `00-summary` 把两件**没做的事**记成「✅ 已补」 | 差点以为完工 |
| 2 | 财务口径改了 8 条，`docs/2026-09-17-home-analysis.md` 的分析没跟上 | 后来照旧文档做事会做反 |
| 3 | 财务面板界面连改 5 笔，`docs/财务管理使用手册.html` 完全没动 | 手册教用户点**已经不存在**的按钮 |

三次的共同点：**都是等用户问起才想起来**。「等问」等于「没有」。

### 具体怎么做

1. **提交前先查一遍**：这笔改的行为，在哪些文档里被描述过？
   ```bash
   grep -rn "<被改的函数名 / 字段名 / 按钮文案>" docs/ *.md
   ```
2. **改了就在同一笔提交里改文档**。提交信息里点明「顺带更新了哪个文档」。
3. **找不到任何文档描述过它** —— 那也值得记一笔：说明这块**本来就没文档**，
   尤其是行为复杂的地方（口径、公式、状态机）。
4. **清单/审计类文档的状态列**（已做 / 未做 / 已补）**必须逐条复核**才能改，
   不能凭「我派过这个活」就写「已补」。**派活 ≠ 做完，做完 ≠ 做全。**

---

## 这个项目的其它既定做法

### 逆向：切源码跑，不要手抄

凡是要跟旧版逐字对照的地方，**把旧版源码切出来真的跑**，不要「读一遍再照着写」——
手抄就是转写错误。已有的公共件：

| 件 | 用途 |
|---|---|
| `docs/home-audit/legacy-slice.mjs` | 切**混淆过的前端 bundle**（含局部解码器多轮闭包） |
| `docs/legacy-finance/lib/run-legacy-fn.mjs` | 切**旧服务端的 TS**（esbuild 剥类型），配 Prisma 桩可跑带 DB 的函数 |

### 验收：差分台，不是「我觉得对」

改旧版相关行为时，建一台**差分台**：左边跑旧版真代码，右边打我们的真接口，逐字段比。
已有一组：`docs/legacy-finance/{05,06,07,08,09}-*.mjs`、`docs/home-audit/*-logiccheck.mjs`。

⚠️ 差分台的**夹具**只有在能说清「夹具本身与旧版口径不符」时才能动，
且必须在脚本注释里写明理由 —— **不许为了让测试变绿而改夹具**。

### 旧版不一定对

旧版里有**死代码**、**自相矛盾**、**能薅的漏洞**。遇到两边不一致时，
先判断是「我们算错」还是「旧版本身有毛病」，**不要默认旧版是对的**。
不照抄的地方要写进注释并说明理由（例：旧版「优惠」前后端是两个公式，且前端那个能被薅）。

### 发现旧版有对应实现时，先整段读完再改

如果用户指出某处和旧版不一样，**先把旧版那一段整个读完**（含入口函数、初值、事件回调），
再一次改完 —— 不要「改一处、等下一次指出、再改一处」。
（教训：财务面板「预付款分配」弹窗因为这个来回改了四轮，本来一轮就能改完。）

### 安全

- 旧版生产环境凭据**只读**使用，**绝不**写进任何文件、提交或日志。
- 要终止进程时**只用具体 PID**，**不用 `pkill -f`**（曾经误杀过用户的后端）。
- 破坏性操作（删库、rm -rf、force push）先问。

---

## 关键文档在哪

| 文档 | 内容 |
|---|---|
| `legacy/DESIGN-DOC.md` | 旧系统业务设计（逆向起点） |
| `docs/2026-09-17-home-analysis.md` | Home 订单管理页的完整逆向 |
| `docs/2026-09-17-home-print.md` / `-receipt.md` | 打印链路 / 电子回执单 |
| `docs/home-audit/` | Home 页保真度审计（含 `00-summary` 的结论表） |
| `docs/legacy-finance/` | 财务口径逆向 + 差异清单 + 差分台 |
| `docs/财务管理-改动核对清单.md` | 财务面板改动，逐条可对着页面核 |
| `docs/财务管理使用手册.html` | **给厂里用的人看的**操作手册 |
| `docs/frontend-conventions.md` | 前端布局规范（响应式、flex-wrap） |
