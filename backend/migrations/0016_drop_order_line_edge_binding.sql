-- 撤销 0013 引入的 edge_binding 列。
--
-- 0013 的判断有误：并非「casing 被复用为套线长度模式，包边另立一列」，而是**原版只有一个
-- 「套线种类」字段**——平开行的「包边:」标签绑的就是它（Hui.formatted.js:2132
-- `modelValue: e["套线种类"]`，候选 `ref(["双包","外包","内包","平框"])` @758）。
-- 该字段一身三职：① 单据展示（平开 = 套线种类+开向 @11881；移门外框 = `套线名：`+套线种类 @12519）；
-- ② 算料激活套线件（`part.track === 套线种类`）；③ 套线长度模式（`-N` 后缀 / 一高一宽… @4266）。
--
-- 新版 line.casing 已完整承载这三职，edge_binding 从未被任何输出路径读取（只在 UI 下拉里写入），
-- 于是「包边填了但单据不显示」。故并回 casing 并删除该列。

ALTER TABLE order_lines DROP COLUMN IF EXISTS edge_binding;
