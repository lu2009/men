# `/hui`（汇算下单·制作回执单）**外壳**审计：工具栏 / 订单头 / 弹窗

- 日期：2026-09-19
- 范围：`/hui` 的**页面外壳**。**明细表不在本文范围** —— 那两张表（列/单元格/编辑态/引擎）
  已有 `docs/2026-09-08-hui-table-gap.md`、`docs/2026-09-18-detail-sfc-recon.md`、
  `hui-engine-logiccheck.mjs`（56 条）、`hui-extract-movecheck.mjs` 在管。
- 起因：用户「继续核实 /hui 页面」。此前**外壳**（工具栏 / 订单头 / 20+ 弹窗）没有任何对照。

---

## 0. 结论

| # | 差在哪 | 性质 |
|---|---|---|
| 0 | 订单头缺 **「备注:」**（订单级备注录不进去） | ✅ **已修** 见 §8.1 |
| 1 | 旧版工具栏第二行有**「视频」**，我们没有 | ✅ **已修** 见 §3 |
| 2 | 旧版有**「调试日志」**弹窗（`title:"调试日志"`，字面量） | **缺口** |
| 3 | 我们有一个旧版**没有**的**「更多功能 ▾」**菜单（14 项） | 结构性偏离 |
| 4 | 我们的 **收款码设置 / 列显隐设置 / 订单列表** 三个弹窗，旧版**找不到** | 我们多的（见 §4） |
| 5 | 我们的 `3D画图` 是 `disabled`；旧版是 `type:"success"` 且活的 | 3D 暂缓所致，表意合理但属偏离 |
| 6 | ~~订单头两个 `input-number`：旧版 `controls:!1`~~ | ✅ **已修** 见 §8.9 |

**已核一致** ✓：工具栏前三颗（`1.清空` / `2.添加门类` / `3.保存回执单`）**文案逐字一致**；
订单头表单 8 个标签逐条对得上；「视频教程」「加价项目管理」等旧版弹窗标题已解出备查。

下面用 `H:行号` 指代 `legacy/js/Hui.formatted.js`。

---

## 1. ⚠️ 先说方法：这个文件**不给文案统一口径**

审计过程中差点因此误判两次，先记下来：

1. **同一个工具栏里，有的文案走解码器、有的是裸字面量。**
   `1.清空` 是 `a(268)`、`2.添加门类` 是 `a(909)`、`3.保存回执单` 是 `a(776)`；
   而 **`刷新`、`视频` 是裸字面量**（`H:12866` / `H:12869`）。
   ⇒ 只扫解码器 token 会**漏掉整颗按钮**（我第一轮就漏了「刷新」「视频」，
   差点写下「旧版没有刷新」）。
2. **一个 chunk 里有 14 套独立的解码器**，各组件一套。
   只解页面那一套（`_0x250a`）是不够的 ——
   「开向模式」这类字符串在**另一个组件**的解码器 `_0x2d32` 里。
   ⇒ 找「某字符串在不在这个 chunk 里」必须**遍历所有解码器 + 同时扫字面量**，
   本文的结论就是这么扫出来的（脚本见 §7）。

---

## 2. 工具栏：旧版两行 **6** 颗，**没有「更多功能」**

```
H:12850-12863   第一行：
  [1.清空]        a(268)   class:"custom-button-btn"  onClick:_0x2f064c
  [2.添加门类]     a(909)   onClick: 置 _0x5b48c7.value=!0（开抽屉）
  [3.保存回执单]   a(776)   onClick:_0xafd9e0
  [3D画图]        a(755)   type:"success"  onClick:_0x45c27e   ← 是**活的**
H:12864-12870   第二行（这两颗是**裸字面量**）：
  [刷新]                   onClick:_0x43542e
  [视频]                   onClick:_0x15bfab  →  置 _0x3b5987.value=!0（开「视频教程」抽屉）
```

**我们**：`1.清空 / 2.添加门类 / 3.保存回执单 / 3D画图(disabled) / 刷新 / 更多功能 ▾`

⇒ 两颗差异：旧版的「视频」我们**没有**（见 §3）；我们多了一个 **14 项「更多功能」**（见 §5）。
`3D画图` 旧版是绿色的活按钮，我们是 `disabled` + tooltip —— 3D 暂缓，表意合理，**但属偏离，记一笔**。

---

## 3. 缺口一：**「视频教程」抽屉**（9 条，已解全）

`H:13338` `title:a(949)="视频教程"`，由「视频」按钮（`_0x15bfab`）打开，
内容取自 `_0x1ca662` 的**名字 → 抖音链接**表（`H:12872` 起），共 **9 条**：

| 教程 | | 教程 |
|---|---|---|
| 制作回执单 | | 加价项目-超墙厚 |
| 添加门图 | | 加价项目-超高超宽 |
| 加价项目-常规操作 | | 加价项目-轨道超长 |
| 加价项目-修改删除 | | 加价项目-单次添加 |
| | | 加价项目-超平米 |

**我们 `views/Hui.vue` 里 `视频`/`douyin` 出现 0 次**，14 项「更多功能」里也没有沾边的项。

> 与 Diao 页那颗「视频」（`docs/2026-09-19-diao-audit.md` §2.1b）是**同一形态**：
> 一张名字→链接表 + 抽屉 + `window.open`。那边我们**已经实现**（10 条逐条一致），
> 所以这里要补的话**是有现成做法的**，不是新造。

**✅ 2026-09-19 已补**（用户拍板）：工具栏「刷新」之后加了「视频」按钮，
抽屉 `title="视频教程"`、`placement="right"`、`:width="300"`（旧版 `size:"300px"`）。

⚠️ **一个差点抄错的点**：**渲染顺序 ≠ 源码里那张表的顺序**。
表（`_0x1ca662`）里 `加价项目-修改删除` 排在 `加价项目-单次添加` **前面**，
而渲染（`H:13341-13375`）是 **`单次添加` 在前**（顺序：制作回执单 · 添加门图 ·
加价项目-常规操作 · **单次添加** · **修改删除** · 超墙厚 · 超高超宽 · 轨道超长 · 超平米）。
照表顺序抄会**错位**。我们按**渲染顺序**实现。
（`docs/2026-09-18-detail-sfc-recon.md` 里那句「按行号全量扫」的教训在这里同样适用：
**看到一张表不等于看到了它的呈现顺序**。）

条目与链接已**机器比过**：9 条**逐条一致**（名称与链接都相同）。
旧版点条目还会补一句 `ElMessage.info("正在打开"+名字+"视频教程")`，我们只 `window.open`
（与 Diao 那颗的处理一致）。

---

## 4. 缺口二：**「调试日志」弹窗**

`H:13025` `title:"调试日志"`（裸字面量）。我们的 11 个弹窗里没有它。
⚠️ 本次**只扫到标题**，**内容与触发入口没核**（见 §6）。

---

## 5. 旧版**没有**、我们**有**的

### 5.1 「更多功能 ▾」（14 项）—— 旧版整个没有这个入口

解码器**全表**（14 套逐个扫）+ 全 chunk 字面量，**搜不到「更多功能」**。
旧版那些入口是**散落**的：例如 `H:12880` 起有个宽 `a(321)="1180px"` 的抽屉，
里面按 `_0x85a3a.value` 取 1/2/8/9 等模式条件地显示
`云打印`(a(736)) / `编辑生产单`(a(461)) / `生成扣板`(a(277)) / `编辑玻璃合片单`(a(393)) 这类。

⇒ 我们把散落的入口**收进一个菜单**。**这可能是合理的信息架构简化，但它是偏离，不是复刻。**

#### 14 项**逐项**落点（已扫完）

扫法：遍历 chunk 里全部解码器（**精确 + 子串**两种）+ 全 chunk 字面量。

| 落点 | 项 |
|---|---|
| ✅ 旧版里有（5） | 玻璃合片单 `_0x250a(709)` · 玻璃订单 `_0x250a(727)` · 加价项目管理 `_0x250a(802)` · 自动加价设置 `_0x250a(1165)` · 排序方式 `_0x250a(524)` |
| 🔶 名字略有出入（2） | **生产单定制打印** → 旧版是「生产单**定制**」（无「打印」二字）；**开向模式设置** → 旧版是「开向模式」（字面量 `H:2749`） |
| ❌ 旧版**连子串都搜不到**（7） | **订单列表 · 模板预览 · 标签打印 · 生产单3打印（双联） · 终端链接 · 收款码设置 · 列显隐设置** |

⚠️ ❌ 那 7 项**不只是"名字不一样"** —— 我用子串扫过 `订单列表 / 模板预览 / 标签打印 / 终端 / 列显隐 / 收款码 / 打印`
等关键词的全部解码结果，**没有任何一条命中**。它们要么是我们自建的入口，
要么对应的旧版能力藏在别的 chunk（本次**只扫了 `Hui.formatted.js`**）。
**要坐实"旧版没有"，还差一步：把 `legacy/js/` 其余 chunk 也按同样方式扫一遍。**

### 5.2 三个弹窗：收款码设置 / 列显隐设置 / 订单列表

遍历 14 套解码器 + 全 chunk 字面量 + `legacy/js/` **其它 chunk**，
**「收款码」「列显隐」「订单列表」三个词一处都没有**。

而且我们自己的注释就写着出处，**承认了它们不是照抄**：

| 弹窗 | 我们代码里的注释 |
|---|---|
| 收款码设置 | 「对应原版 `getImage('qrcode')`」—— 对应的是旧版的**一个取图接口**，不是旧版的**弹窗** |
| 列显隐设置 | 「两表共 40 多个复选框，**比旧版多**，必须限高可滚」—— **自己写明比旧版多** |
| 订单列表 | 附近无出处注释 |

⚠️ **精确说法**（别说过头）：
- 「收款码」在旧版是个**数据概念**（`getImage('qrcode')`），**弹窗是我们做的**；
- 「列显隐」在旧版是个**数据概念**（`ping_column` / `diao_column`，租户级列开关），
  **弹窗是我们做的、且比旧版多**；
- 「订单列表」旧版整个 chunk 里找不到对应物 —— **这条最需要再确认**（见 §6）。

---

## 6. ⚠️ 本文**没有**覆盖的（别当成已核）

1. **弹窗内容**：除标题外，**字段 / 默认值 / 确认时的校验与写入**一律没核。
   （派了 `hui-dialogs` 小分队去盘，**它没交报告**，见 §7。）
2. **「添加门类」抽屉**：只扫到工具栏按钮 `a(909)="2.添加门类"`，
   **抽屉本身的 `title:` 没在页面区间扫到**，内容也没核。
3. **「更多功能」14 项** 逐项在旧版的落点。
4. **「调试日志」** 的触发入口与内容。
5. **订单头表单的绑定键**（`_0x2bcafc`=生产天数 / `_0x3c4d97`=订金 / `_0x5220a4`=品牌 /
   `_0x184c0c`=日期 …）逐个核对。
   ——「编号」见 `H:13102`（`a(674)` + `_0x48f822`）。

### ✅ 已补扫：「截止日期」旧版**没有**这个概念

我们订单头那行 `截止 {{ dueDate }}`（`Hui.vue` 的 `dueDate` = `order_date + 生产天数 + 1 天`），
**在旧版里找不到对应物**：扫遍全部解码器，`交货 / 交期 / 完工 / 预计 / 工期 / 截止` **一个都没有**
（只有「生产天数」「生产天数:」）。我们那颗 `dueDate` 上也**没有出处注释**。
⇒ **这是我们自建的显示**。旧版只有「生产天数」这个输入，不显示算出来的截止日。
要保留就得写成**有意偏离**；不保留就删掉。**需要拍板，我不擅自改。**

---

## 7. 怎么复现本文的每一条

```bash
cd /Users/aaa/Desktop/door-main

# ① 解码器（页面那套）：数组函数 _0x1ee4 + 解码函数 _0x250a + 轮转 IIFE
#    ⚠️ 轮转 IIFE 必须带上，否则索引整体错位。切函数要**按花括号配对切**，别用 sed 截断。
#    现成的：/tmp/dec.mjs  node /tmp/dec.mjs 268 909 776 755
#    自检：566="includes"、847="parentSubsidiary"

# ② 遍历**所有**解码器（本次 14 套）找某个字符串 —— /tmp/all-dec.mjs
#    只扫一套会漏（「开向模式」在 _0x2d32 那套里，不在页面的 _0x250a 里）

# ③ 全 chunk 的字面量也要扫（「刷新」「视频」「订金(元):」「调试日志」都是字面量）
grep -n '刷新\|视频\|调试日志' legacy/js/Hui.formatted.js

# ④ 页面区间的所有 title：
#    12850-13436（页面 render）内 title: 只有 7 个，见 §0 表
```

⚠️ 小分队情况记录：本次派了 3 支（`diao-topbar` / `hui-shell` / `hui-dialogs`），
**全部只发 idle、一份报告都没交**。本文结论**都是我自核的**，没有一条来自转述。

---

## 8. 弹窗内容（2026-09-19 补扫）

扫法：按行 dump 页面区间（`12850-13436`）里**每条解码 token + 中文字面量**，
一次拿到标题/字段标签/按钮文案/占位符的清单（脚本见 §7 ⑤）。

### 8.1 ⭐ 新缺口：订单头缺 **「备注:」**

旧版订单头表单（`H:13051-13102`）逐项：`客户`（占位「输入客户信息」）· `电话:`(130px) ·
`安装地址:` · **`备注:`** · `业务员:`(90px，占位「业务员名称」) · `生产天数:`(60px) ·
`订金(元):`(70px) · `品牌:` · `日期: ` · `编号:`。

**我们那行没有「备注:」**（`Hui.vue` 的 `header-form` 只有客户/电话/安装地址/业务员/
生产天数/订金/品牌/日期/编号）。我们的 `order.remark` 字段**存在**（`:575`），
但只在**行级**（`:1527` 明细行的 remark）和保存时用，**订单头没有输入框**。

⇒ **订单级备注录不进去。** 这是本次扫出来的最实的一条。

> **✅ 2026-09-19 已补**（用户拍板先做这条）：在「安装地址」与「业务员」之间加了一格
> `备注:`（`n-input`，宽 **150px**，绑 `order.remark`）。
>
> ⚠️ 补之前先把旧版那个条件弄清了：这一格挂着 `_0x1b58e4.value ? … : 注释节点`，
> 而 `_0x1b58e4`（`H:7828` 初值 `Vue.ref(!0)`）**只在 `defaulted === 3`（终端账号）
> 那一支被置 `false`**（`H:8218`），它同时门控着某个弹窗里的「微信分享(手机)」等按钮
> —— 即「**非终端账号**」。**终端模式我们不做** ⇒ 对我们**恒为 true**，
> 所以**无条件显示**是对的，不是漏了条件。理由写在代码注释里。

（顺带：几处宽度也不同 —— 旧版 电话 130px / 生产天数 60px / 订金 70px；我们 110/96/110。小差异。）

### 8.2 「调试日志」弹窗（我们没有）

`H:13020-13049`：宽 **1123px**、`margin:0 auto`；三颗按钮
**`关闭` / `清除日志` / `测试热更新（Capacitor）`**；空态文案 **「没有日志记录」**。

### 8.3 加价三兄弟（已核，与我们基本一致）

| 弹窗 | 旧版 | 我们 |
|---|---|---|
| 加价项目管理 | `:13107`；两颗入口 **`新增加价项目`(primary) / `修改/删除加价项目`(warning)** | ✓ 文案一致 |
| 新增加价项目 | `:13119` 宽 `500px`；按钮 `取消` / `确认`；字段 **`加价项目`(name，占位「请输入加价项目名称」) / `单价`(price) / `计价方式`(unit)** | 待逐字段核 |
| 修改加价项目 | `:13164`；按钮 **`取消` / `删除`(danger) / `确认修改`(primary)**；多一个 **`选择项目`(selectedIndex，占位「选择修改或删除的项目」)** | 待逐字段核 |

⚠️ 注意标签是 **「单价」**，不是「价格」；第三个字段叫 **「计价方式」**。

### 8.4 自动加价设置 / 排序方式（旧版有说明文案，易漏）

- **自动加价设置**（`:13228`，宽 `320px`，按钮 `取消`/`保存`）里有一颗勾选 **「去除自动加价」**，
  下面跟说明 **「勾选后，玻璃和尺寸相关加价项目将不再自动选中」**（`:13241`/`:13246`）。
- **排序方式**（`:13248`，宽 `320px`，按钮 `取消`/`保存`）是**单选**：**`型材优先`(profile) / `序号优先`(order)**，
  说明 **「序号优先：按订单号从小到大排列」**（`:13264`/`:13268`/`:13274`）。

### 8.5 ⚠️ §5.1 要**修正**：旧版**有**菜单，只是不叫「更多功能」

补扫发现旧版工具栏区还有**几个下拉**（`H:13300-13336`）：

| 旧版下拉 | 内容 |
|---|---|
| `总余额显示`（`:13304`） | 开关 + 说明「开启后将在回执单中显示客户总余额」(`:13309`) |
| `排序方式`（`:13315`） | 入口（开 §8.4 那个弹窗） |
| `辅助菜单设置`（`:13322`） | 开关 **「开启手机辅助菜单」**(`:13327`)、**「全面屏（不预留底部空间）」**(`:13333`) |

⇒ **§5.1 说「旧版整个没有这个入口」是不准确的**：旧版是**若干个小的下拉/开关**，
我们收成了**一个 14 项的「更多功能」**。**收编这件事本身仍是偏离，但不是"无中生有"**，
而且这里还有两条**旧版有、我们完全没有**的：**「总余额显示」开关**、**「开启手机辅助菜单 / 全面屏」**。
（`总余额显示` 对应的能力我们在别处有 —— `ReceiptCard` 那块；但**入口**没复刻。）

### 8.6 「添加门类」抽屉（已扫完）—— 我们少两颗入口、宽度/关闭钮也不同

`H:13276-13298`：`direction:"rtl"`、`size:"200px"`、**`show-close:!1`（没有 × 关闭钮）**。
里面 **5 项**：**`平开门` · `移门` · `导入上次订单`(warning) · `加价项目管理` · `自动加价设置`**。

**我们**（`Hui.vue:142-155`）：`width 240`、**`closable`（有 ×）**，只有 **3 项**
（`平开门` / `移门` / `导入上次订单`）+ 一句自加的提示文案。

⇒ 三处差异：**宽度 200→240**、**旧版无关闭钮→我们有**、
**少 `加价项目管理` / `自动加价设置` 两颗**（这两颗我们挪进了「更多功能」，
所以是**搬家**不是缺功能 —— 但「抽屉里也有」这件事旧版是有的）。

### 8.7 订单头字段的**绑定键**（已扫完，完整映射）

| 字段 | 旧版行 | 绑定 | 我们的 |
|---|---|---|---|
| 客户 | `:13049` | `_0x340df0` | `order.client_code` |
| 电话 | `:13061` | `_0x546bb0` | `order.phone` |
| 安装地址 | `:13067` | `_0x17ac36` | `order.install_address` |
| **备注** | `:13073` | **`_0x2f1634`** | **我们没有这一格**（见 §8.1） |
| 业务员 | `:13079` | `_0x4fc122` | `order.salesperson` |
| 生产天数 | `:13085` | `_0x2bcafc` | `order.production_days` |
| 订金 | `:13091` | `_0x3c4d97` | `order.deposit` |
| 品牌 | `:13097` | `_0x5220a4` | `order.brand` |
| 日期 | `:13101` | `_0x184c0c` | `order.order_date` |
| 编号（只读） | `:13102` | `_0x48f822` | `order.receipt_no` |

⇒ 旧版**确有独立的「备注」字段**（`_0x2f1634`），不是我们猜的「行级备注」——
**§8.1 那条缺口坐实**。

### 8.8 本文至此**已覆盖**的范围

✅ 工具栏（6 颗 + 3 个下拉）· 订单头（10 格含绑定键）· 页面区间全部 7 个 `title:` 弹窗的内容 ·
「添加门类」抽屉 · 全部 14 项菜单落点 · 「截止日期」的归属。

⚠️ **仍未覆盖**（如实列出）：
1. 「自动加价设置」「排序方式」**我们这边**的字段/选项是否逐字对上（旧版那侧已 dump，见 §8.4）；
2. 「调试日志」旧版**怎么触发**（三颗按钮里「测试热更新（Capacitor）」是给 App 的，
   桌面端点了会怎样**没核**）；
3. 「测试热更新」等按钮在我们的技术栈里**该不该复刻** —— 属产品决定。

### 8.9 ✅ 已修：订单头两个 `input-number` 的 `controls` 与宽度

| 字段 | 旧版 | 我们（改前） | 改后 |
|---|---|---|---|
| 生产天数 | `min:1, controls:!1`, 宽 `a(639)="60px"`（`H:13084`） | `:show-button="true"`, 96px | `:show-button="false"`, **60px** |
| 订金(元) | `min:0, controls:!1`, 宽 `a(1011)="70px !important"`（`H:13090`） | `:show-button="true"`, 110px | `:show-button="false"`, **70px** |

旧版这两个是**纯数字输入框、没有上下箭头**；我们开了箭头、且宽了不少。
（顺带：我们**别的** `input-number`（加价那两处）本来就是 `:show-button="false"`，
所以这一改反而是把订单头这两格**拉回本仓库既有口径**。）

---

## 9. 「总余额显示」开关 —— **已拍板：方案 A**（2026-09-19）

用户拍板，**下一轮实施**。这里记全，免得重推。

### 9.1 旧版是什么

`H:13299-13312` 一个**下拉**：按钮文案「 总余额显示 」，开着时后面跟一个 **✓**（按钮 `type` 随之变）；
点开是 200px 浮层，里面有提示语 **「开启后将在回执单中显示客户总余额」**（`a(730)`）
+ 一个 `el-switch`（`active-text:"开"` / `inactive-text:"关"`），绑 `_0x2ffe36`。

onChange `_0x14b5ca`（`H:7934`）：
`localStorage.setItem("showTotalBalance", String(_0x2ffe36.value))` + 一句成功提示。

### 9.2 我们这边的现状（已核）

| 块 | 现状 |
|---|---|
| 开关 UI | ✗ 没有 |
| `localStorage['showTotalBalance']` | ✗ 搜不到 |
| 回执侧渲染「客户总余额」 | ✗ `receiptBuilder.ts` / `receipt2/*` 里没有「余额」 |
| **余额数据本身** | ✅ **有** —— 后端 `finance/service.rs:197` `customer_balance`（`GET …/balance`），前端 `FinanceDrawer.vue` 在用 |
| **回执载荷里有没有余额** | ✗ **没有** —— `backend/src/modules/receipts/service.rs` 只带 `client_name` 等 |

### 9.3 ⚠️ 拍板的关键：**分享页是无认证的**

回执在**三个**入口渲染：`ReceiptView.vue`（要登录）· **`ReceiptShare.vue`（`/receipt-share`，客户扫码看，无认证）** · 打印。

财务余额接口要鉴权 ⇒ **分享页自己拉不到余额**。旧版没这问题（选中客户时就把余额读进页面内存，前端渲染）。

**用户 2026-09-19 选定方案 A**：**余额随回执载荷由后端下发**（`receipts/service.rs` 里 join 财务），
**分享页也显示**。

> ⚠️ **这条决定有隐私含义，用户知情并拍板**：等于**把客户余额给到任何持有分享链接的人**。
> 实施时**不要在代码里把它写成"技术细节"** —— 它是一个明确的产品/隐私取舍，
> 注释里要写明「用户 2026-09-19 知情拍板，选了 A」。

### 9.4 实施清单（下一轮）

1. **后端**：`modules/receipts/service.rs` 的载荷加余额（join 财务口径，注意别另算一套 —— 直接复用 `finance/service.rs` 的 `customer_balance`）；分享令牌那条无认证路径同样要带。
2. **前端开关**：`Hui.vue` 工具栏加下拉（文案/✓/浮层提示语/`el-switch` 开-关 全部照 `H:13299-13312`），绑一个 ref，onChange 写 `localStorage['showTotalBalance']`（键名与旧版**逐字相同**）。
3. **回执渲染**：`receiptBuilder.ts` / `receipt2/*` 里按开关决定是否出现余额行；三个入口都要生效。
4. **差分台**（**这条唯一能证明对了的方式**）：把旧版 `_0x2ffe36` / `_0x14b5ca` 那两个分支切出来真跑，逐例比「开关 on/off 时回执该不该出现余额行、显示什么」。

⚠️ 别跳过第 4 条 —— 这条**没有现成差分台可复用**（`print-lineno-check.mjs` 钉的是单号取哪一级，不是这个）。
