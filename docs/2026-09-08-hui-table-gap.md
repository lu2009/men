# 明细表逐格对照：旧版 vs 当前实现

> 数据来源：
> - 旧版：`legacy/js/Hui.formatted.js` 反推（平开组件 `_0x5a7707` 渲染 1698–2818；吊趟/移门组件 `_0x4d18bf` 渲染 4491–5567）。
> - 当前：对运行中 `/hui` 两张 `.lines-card` 表 DOM 实测（表头与单元格控件）。
> 结论先行：**当前行内编辑已能“直接在表里改”，但列的数量/组合/顺序/密度和旧版不同，观感差异主要来自“旧版一格里塞多个小控件、列窄而密；我们一列一控件、列宽而疏”。**

---

## 1. 平开门表

### 1.1 旧版平开表（可见列按顺序）
| # | 列 | 格子里是什么 |
|---|---|---|
| 1 | 日期/回执单号 | 常隐（`he`=false） |
| 2 | 操作列（表头=平开门，宽~50） | 复选框(批量)；行内：**删除 / 复制 / 查看3D**（link 按钮）；待保存时替换为 **确认修改/取消** |
| 3 | 门花图(宽~50) | 有图=缩略图(可放大)+删除；无图=点击传图 / 文字传图 / 算料 |
| 4 | 型材/颜色 | 型材 autocomplete + 颜色 autocomplete（一列两控件，纵排） |
| 5 | 单价/数量 | 单价、数量 两个数字输入（一格两控件） |
| 6 | 玻璃 | 面玻 / 底玻 / 玻璃厚 三个 autocomplete |
| 7 | 开向（无表头文字，一格四行） | 「包边:」套线种类、「锁具:」轨道种类、「开向:」开向下拉、开向示意图 img |
| 8 | 门洞尺寸 | 门洞高 / 门洞宽 + 墙厚（+钻石型变体、洞尺下拉「洞尺/净尺」） |
| 9 | 吊脚 | 数字输入 |
| 10 | 亮窗总高 | 数字输入 |
| 11 | 五金 | 多选 el-select（allow-create） |
| 12 | 封板高 | autocomplete（板高历史） |
| 13 | 备注 | 地址 + 备注(textarea) |
| 14 | 金额 | 金额(只读) + 平方数(只读，右键改) |
| 15 | 加价项目 | 「点击添加:」+ 多选 + 选中项明细多行 |
| 16 | 计价方式 | autocomplete 套/方 |
| 17 | 打折 | 数字 |
| 18 | 前包加长 / 后包加长 | 数字输入 |
| 19 | 单/双丁墙体 | el-select（单丁墙/双丁墙/上丁/…/正常） |
| 20 | 单号 | el-input（条件列） |
| 21 | 图片ID/客户/客户编号/其它费用 | 常隐 |

### 1.2 当前平开表（实测列序）
操作 → 门图 → 型材/颜色 → 数量 → 单价 → 玻璃 → 开向 → 轨道 → 套线种类 → 包边 → 门洞宽 → 门洞高 → 墙厚 → 亮窗总高 → 吊脚 → 计价 → 打折 → 平方 → 金额 → 加价 → 备注

| # | 列 | 格子里是什么（实测） |
|---|---|---|
| 1 | 操作(156) | 编辑/复制/删除（纵向 link） |
| 2 | 门图(92) | 无图=虚线块「传图/文字」；有图=缩略+放大+× |
| 3 | 型材/颜色 | 两 input 纵排 |
| 4–5 | 数量 / 单价 | 各一个数字输入（**拆成两列**） |
| 6 | 玻璃 | 面玻/底玻/玻璃厚 三个下拉（横排一格） |
| 7 | 开向(132) | 开向下拉 + 下方开向图（**没有内嵌 包边/锁具**） |
| 8–10 | 轨道 / 套线种类 / 包边 | 各自独立列（旧版藏进「开向」列内嵌小label） |
| 11–15 | 门洞宽/门洞高/墙厚/亮窗总高/吊脚 | 各一列数字输入 |
| 16–17 | 计价 / 打折 | 独立 |
| 18–19 | 平方(只读右键改) / 金额(只读) | **拆成两列**（旧版同格） |
| 20 | 加价 | 摘要文本按钮（点开进行编辑） |
| 21 | 备注 | 单行 input |

### 1.3 平开表差异
1. 操作列：旧版有 **复选框(批量)** 与 **查看3D**；我们无（3D 已定后置）；多一个「编辑」入口。
2. **缺列**：五金、封板高、前包加长、后包加长、单/双丁墙体、单号 —— 全部要进抽屉，未行内。
3. 旧版「开向」一格内嵌 包边/锁具/开向+图；我们拆成 轨道/套线种类/包边/开向 四列 → 更宽更疏。
4. 单价/数量、金额+平方 旧版**同格两控件**，我们拆列。
5. 密度：旧版格子 padding 1px、窄列、一屏几十列；我们列宽大、输入框 padding 大 → 明显更“空”。
6. 校验表现：旧版缺必填字段**单元格红框 error-cell**；我们只弹消息。
7. 已录行删除：旧版“验密码→确认→已录单再确认→deleteRow”；我们只有“已录单二段确认”。
8. 加价：旧版行内多选下拉 + 明细多行 +「加价项目管理/自动加价设置」入口；我们是摘要按钮+抽屉逐条。

---

## 2. 移门/吊趟表

### 2.1 旧版移门表（可见列按顺序）
| # | 列 | 格子里是什么 |
|---|---|---|
| 1 | 日期/回执单号 | 常隐 |
| 2 | 移门操作列(50) | 复选框 + 删除 / 复制 / 查看3D；待保存=确认修改/取消 |
| 3 | 门花图(50) | 缩略/点击传图/文字传图/算料 |
| 4 | 型材/颜色 | 两 autocomplete |
| 5 | 单价/数量 | 单价 + 数量 + **套线单价**（一格三控件） |
| 6 | 玻璃 | 面玻/底玻/厚度 |
| 7 | 扇数/开向 | 扇数 autocomplete + 开向 autocomplete + **组合开向示意图 img** |
| 8 | 下轨道/套线 | 轨道种类 + 套线种类 |
| 9 | 门洞尺寸 | 高/宽 + 墙厚（+洞尺下拉） |
| 10 | 亮窗信息 | 亮窗总高 + 亮窗数量（+封板高条件） |
| 11 | 五金 | el-tag 列表 + 新增 |
| 12 | 备注 | 地址 + 备注 |
| 13 | 金额 | 金额(只读)+平方(右键改) |
| 14 | 加价项目 | 点击添加 + 多选 + 明细多行 |
| 15 | 上轨/边封 | 轨道长 + 边封数 |
| 16–17 | 前包/后包加长 | 数字 |
| 18 | 单双丁 | select |
| 19 | 计价方式 / 打折 | autocomplete / 数字 |
| 20 | 单号 | 条件列 |
| 21 | 其它费用等 | 常隐 |

### 2.2 当前移门表（实测列序）
操作 → 门图 → 型材/颜色 → 扇数 → 开向 → 数量 → 单价 → 套线单价 → 玻璃 → 轨道 → 套线种类 → 门洞宽 → 门洞高 → 墙厚 → 亮窗总高 → 吊脚 → 亮窗数量 → 轨道长 → 边封数 → 计价 → 打折 → 平方 → 金额 → 加价 → 备注

### 2.3 移门表差异
1. 与平开类似：操作列无复选框/查看3D；缺 五金、封板高、前包/后包、单双丁、单号、洞尺；轨道长/边封数我们有但位置靠后。
2. **组合开向示意图缺失**（旧版扇数+开向一格内直接有图）——旧版是动态绘制，目前后置（C）。
3. 单价/数量/套线单价旧版同格，我们拆三列。
4. 扇数、开向 我们各一列（开向列没有组合图）。
5. 密度/间距、校验红框、密码层、加价形态差异同上。

---

## 3. 观感差异主因（优先级排序）

| 优先级 | 差异 | 影响 |
|---|---|---|
| 高 | **密度与“一格多控件”布局**（单元格 padding 1px、多个子控件挤一列、窄列宽）：旧版一张表 20+ 列紧凑；我们宽而疏 | 决定“像不像表格” |
| 高 | **组合列没还原**：单价/数量、金额+平方、开向内嵌包边/锁具 | 结构骨架不同 |
| 中 | **缺列未行内**：五金/封板高/单双丁/前包后包/单号/洞尺（仍需抽屉） | 行内覆盖率不足 |
| 中 | 操作列：复选框(批量)、查看3D(后置)；删除/复制布局 | 交互差 |
| 中 | 校验标红单元格、验密码、未保存行高亮 | 状态反馈 |
| 中 | 加价项目多选+明细多行、「加价项目管理/自动加价设置」入口 | 功能入口差异 |
| 低 | 列顺序、默认枚举（玻璃厚 4,8,5,6,10,12,0 等）、列宽自适应 | 细节 |

> 注：移门组合开向示意图（旧版动态绘制）与 3D 同属 C 期，本表未计入“可直接修”范围；其余差异都是可在现有行内网格上继续对齐的。
